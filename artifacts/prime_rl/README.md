# Prime-RL Artifacts

This directory is for small, committable summaries from Prime-RL runs: metrics
CSVs, JSON summaries, and reward-curve SVGs.

Raw Prime-RL outputs, checkpoints, and rollout dumps belong under
`artifacts/prime_rl/runs/`, which is gitignored.
